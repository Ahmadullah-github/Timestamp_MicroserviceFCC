# Timestamp_MicroserviceFCC
Timestamp Microservice is a simple API that returns Unix timestamps and UTC date strings based on user input. It can handle both Unix timestamps and natural language date formats while also returning the current date when no input is provided
